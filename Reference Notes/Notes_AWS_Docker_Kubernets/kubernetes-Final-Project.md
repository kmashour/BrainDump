![[auth-service-resources.html]]


JWT -- > in Authentication service json web token used by APIs for authentication and also can be used by user, so a user can access the other application micro-services after authentication 