
https://www.youtube.com/watch?v=wTRezbXnlj8

okay I think we have Quorum and we can start I got the thumbs up in the back great so um before we get very far along I have to give you an update my co-speaker uh Michael mun uh couldn't make it at the very last minute to be honest he got covid and is doing the responsible thing and staying home um his topic as the co-speaker was going to be more about the security posture of running connectivity proxy in a kuber cluster and my material is um was going to be more about the experience I had in gke running connectivity at that scale so we've sort of tweaked the slides around to essentially Focus mainly on my content so if anybody feels strongly bait and switched I apologize so getting started um here's the structure of this talk uh I'm going to start with a generic overview of the problems Sol by connectivity proxy and how it works and then I'll go into as a main as a maintainer and this is a maintainer track uh talk I'll go into my personal experience in gke using the proxy okay so um just make sure so just for starters um connectivity is a TCP proxy for cube API server running in the control plane to talk to Cluster uh resources on worker nodes uh this extension is um associated with the API machinery and cloud provider sigs uh it utilizes a server and agent topology it's a it's a forward proxy uh you'll see that later and it's um it's a specific to API server um solution it's not meant to be a generic proxy um also Al I want to mention this presentation will attempt to avoid covering too much repeat information with some previous cubec con talks in 2019 some of the designers and implementers of connectivity gave a nice presentation of uh more of the inner workings and implementation details so I won't get too into the depth here uh and then just last year a couple of presenters talked about some interesting um Rich use cases that they had found for connectivity um yeah they had a nice talk where they talked about remote control plans was the term they used uh so I won't talk too much about the variety of use cases um so getting started the diagram you see uh illustrates the the typical divide of a kubernetes cluster on the left you've got the control plane components um and on the right you've got there's usually one or more worker nodes uh running cluster resources most control plane traffic is client-based and destined to the API server even most control plane components like the scheduler controller manager Etc are API server clients uh etcd is the primary exception to this rule um in a cluster with separate control plane and cluster Networks it's common to firewall off everything in the control plane except API server and make it available to the cluster typically behind a load balance here some the arrows from cblood and pod I'll mention what they are some primary examples of node to control plane traffic include node registration performed by the cuet or a pod-based workload calling the kubernetes API but API server needs to uh some core kubernetes features require that API server have network access to the cluster significantly web Hooks and aggregated API servers are core kubernetes features that are common to run in the cluster on workers so this illustrate this illustration shows uh the other kind of Arrow flowing outward from API server it's um common to have local off authorization endpoints as well as Local web hooks in the control plane but it's also common for the API server to call out to web hooks aggregated API servers also cuet pod node service in the cluster when by default API server when it wants to communicate with the cluster resource it directly dials the relevant IP address this default Behavior assumes that cluster resources are routable from the control plane to worker nodes kubernetes clusters with a network split cannot assume that cluster resources are routable like this that Network split is the problem that connectivity proxy solves and just as a side note some of you may notice that a web hook I already mentioned it you can have web hooks in both control plan and workers and we might get to that later so connectivity proxy this design introduces a connectivity server running in the control plane and made available to the cluster in the case of multiple control plane nodes the connectivity servers are assumed to be put behind a load balancer a connectivity agent is introduced in the cluster as a system pod and on Startup it immediately initializes a long lived grpc connection to the server the grpc connection is secured with m TLS and the server authenticates a given agent via API server token review API finally once the connectivity server and agent are installed and initialized API server can be restarted with new configuration that enables it to use the proxy API server has private config called egress selector configuration when it is used API server looks at the kind of resource it is accessing and according to where that resource is located a proxy aware Network dialer is used it is also possible to use the proxy with traffic to SCD or to the control plane but in this presentation we're focusing on the cluster all the black lines in the diagram are normal TCP connection connections the dotted lines are proxy data connections and control packets that are multiplexed over by bidirectional grpc streaming the two orange arrows are the grpc bidirectional streams brokered by the connectivity Service as a concrete example let's say a user types Cube cuddle port forward to a pod API server requests the proxy system to dial a new TCP proxy connection control packets arrive at the agent which opens a normal TCP connection to the Pod for the final segment API server and the proxy components will continue supporting a full duplex end to end TCP connection until the user closes the connection uh and you can try it out this entire solution is um fully open source and there are instructions at the link to play with it uh so just briefly I'll touch on the topic of security which was going to be um Elmo's topic um there is a historical class of security issues like the one shown that basically involve confused API server thinking it's dialing a cluster resource but it's actually not when API server runs with any cluster proxy configure implementation traffic is effectively constrained into the appropriate trust segment namely into the cluster even though known issues are patched routing control plane to know traffic through a forward proxy enhances the security posture against possible future issues in the same class uh so now to the topic of running connectivity on gke uh first why did gke need connectivity proxy uh historically for a class of gke clusters that we might call public clusters uh we used SSH tunnels which was deprecated in kubernetes open source way back in like version 1.9 and ultimately totally removed in 122 so gke needed a replacement for such clusters before then um more recently clusters with private nodes um have historically used VPC peering directly between the control plan and the worker nodes but there has there's been a class of problems around that that are solved by a newer a newer class cross cluster uh topology that uses Google private service connect uh gke is unifying around clusters to have that and that link is to a blog post that explains that in further detail uh so how does gke run connectivity uh first of all gke uses grpc mode uh there is an alternate HTTP connect mode but we chose grpc because it has the promise of better multiplexing and performance uh one note on that is that it only affects the first tunnel segment it does not affect the tunnel segment between the server and the agent uh another another interesting difference is we run the agent via a deployment rather than than as a Damon set like the example manifests uh it's a more efficient use of resources not just CPU and memory in on the worker nodes but also pod IE IP space uh it would be too expensive to use an unnecessary amount of pod IP space um there's also a scalability limit in that remains in connectivity Cube API server can handle ingke can handle like up to 15,000 worker nodes all registering but connectivity server does not have the same scalability and it can't handle more than a smaller number of Agents initializing that grpc connection so we avoid that scalability we avoid that scalability limit um at the scale of GK Fleet everything has to be automated all cluster maintenance events that may apply connectivity as part of a migration do so in two steps one step to provision the portions and another subsequent step to enable API server to use it uh and this is really critical because uh if the tunnel is not fully ready for due to a variety of factors we don't want to uh take the Second Step which could break some control plane functions um so what have been some challenges of running it uh first of all in 2021 when we first launched uh um it was clear that it's within a user's control to break it uh which is actually pretty noteworthy because the apparent defect is part of the cluster's control plane function it's not always obvious uh that Cube codal logs if it fails to dial was due to something that the user did on the worker network uh in 2022 after SSH tunnels based clusters had been migrated to connectivity we it did encounter some new and uniquely painful issues in one case a thrashing cluster workload included exercising control plane ESS um at such a high rate that the connectivity components became overloaded and it had a cascading effect uh in another case a cluster got into a deadlocked State due to a broken user installed web hook uh that I think happened to be a gate happens to be a gatekeeper web hook but that's not important um but the critical point is that it matched all API groups um and it was broken at some point uh note the circular dependency of the control plane needing a working tunnel to access the user web hook but the tunnel is never able to initialize due to the agent pod's failing admission web hook so it's it's a deadlocked cluster uh and as noted these are these are fairly difficult and painful to debug um failure modes uh and binary version skew particularly across API server to the connectivity binaries is an extra headwind uh so what did we do um in 2022 we had to um invest further in restabilization we also took a look a hard look at there's always the possibility of replacing the reference implementation Parts with a different design using the same API server extension points but in the end we were able to harden the existing connectivity service to meet our reliability expectations um in 2023 we have seen the fruits of those efforts uh and connectivity adoption is increasing as part of private service connect unification migration uh so what's next uh there's an active open source community that meets bi-weekly focused on connectivity both maintenance and the path to GA there are some interesting unsolved problems on the connectivity side for example there is a proposal to simplify the protu the proxy connection identifiers but it would be a significant protocol change in the broader control plane egress space there is an open question of how best to support finer Grand use cases for example identifying subsets of the cluster to dial uh see the links and resources in the PDF if you're curious to learn more or want to get involved and although we're pretty um we haven't used all that much time we have plenty of time for Q&A [Applause] than please andols access it also going uh let me go back to a diagram so this diagram shows Cube cuddle which is assumed to be external to the cluster yeah so it would be going over um there's an assumed publicly accessible but secured endpoint to the Clusters control plane in gke it sort of depends on which configuration you've chosen uh you're it is the same kind of client though as maybe back further in this example where that pod is calling the the the kubernetes API as an internal cluster Kate's client I think it's fully private control plane in a fully private control plane then you would only support this pod use case and you would not necessarily support this direct Cube codal client oh I see uh well partly to answer that question the general answer I would give is no in the connectivity proxy um um enhancement working group there has been a discussion to support routing traffic traic in the reverse direction from outside the control plane into the control plane but that disc that was never in the original scope of the enhancement and that work which even had like some prototype underway was never completed and merged so at this time connectivity is only a proxy for API server to use to Route traffic to one of those three canonical locations at CD control plane or cluster yeah yeah no problem any other questions um I was wondering if you could share you had mentioned one of the challenges you had faced is like customers making a firewall rule or um just like making their worker nodes unschedulable or not allowing the connectivity agent to schedule on their worker nodes how did you guys solve that yeah good question uh so let's see back here so this is an example of that um if if a user um um had very deliberately tight Network egress deny rules or if they had the right kind of taints on all worker nodes um and that there are even other things like binoy is one uh they can prevent the Google managed system pod uh connectivity agent from running um part of the answer is that that's still a remaining um headwind in gke but um for one thing we have user guides that sort of discourage the kind of things that could break a system pod uh and we have Network firewall rule troubleshooting guides that mention oh you know uh by default Network egress is allowed but if you lock down egress by default and only allow Cube APS server Port 443 you need to also allow connectivity server Port 8132 uh for your cluster to be fully healthy thanks makes sense yeah in this type of set is there a way of and policy enforcement on if we are using an agent then there we connecting to the management or any other connectivity should be all directed from the connectivity agent would that automatically be enforced from a policy perspective or would that something that we would have toig toit that the shorter answer is no connectivity is not aware of a great number of things that about the cluster that it could be aware of such as data plane Network policies um in fact I glossed over it but there is an assumption with connectivity um default implementation and design that a given connectivity agent will be able to dial anywhere else in the cluster which may not always be true and there is a configuration of connectivity where if you can run it as a Damon set and if the agent can provide some hints like what IP address ranges it uh serves then there are connectivity server configurations where a routing strategy can have the proxy server pick the exact agent running locally where the dial Target is but in the default out of the box configuration there is an extra interesting hop from the agent to the Pod that might be across nodes in the in the cluster in terms of policy API server is really the only decision maker in this entire design everything Downstream from API server is purely a TCP low-level Network like TCP over grpc streaming Network um tunnel y can you talk a little bit more about the scalability how many AG you can use yeah sure um the scalability limit um some of the tuning parameters uh involve like I think the connectivity agent has a keep alive and there's also there's a part of the protocol that the agent performs in order to support um the number of connectivity servers increas Ing and decreasing uh and that could happen if you wanted to change the number of uh control plane nodes to make your cluster more like either horizontal scalability or um going from single to fault tolerant cluster um the agent supports that Dynamic server count change by sort of continuing to try to connect through the load balancer until it connects to all the Full Count of connectivities servers it learns the count or number to connect to to know when to sort of back off uh via response headers as part of that initialization but even when it catches up to the number of expected it's still on a slower basis polls just in case the number increases right so depending on the tuning of that pole Loop um you can run into I think it's an easily solved problem what you run into is connectivity server itself is a kubernetes client for that token a token review API call and the default client throttling just does not have any sort of push back and we sidestep that scale like so if you hammer it with enough agents at a rapid enough then you can basically get queuing there when um a more GA implementation would um like load Jed or push backs somehow uh and it just hasn't been done uh frankly in gke we did the analysis to see like okay how much connectivity infrastructure do we need to run and how is it related to the size of the cluster and one interesting data point was that the Clusters in the entire fleet that had the highest use of control plane ESS like were like one or two or three node clusters and what was H what was happening is they just had like broken controllers busy looping and so the scalability had nothing to do with the size of the cluster so we basically just scale enough agents to meet any use case that we had like we we allow no more than 100 because in our manual testing we saw that at 100 for the tuning parameters we had used that's where things started to queue up but for most clusters the number is a lot less thank you was there a reason you weren't using domain sockets um I think we do use a Unix socket um so there's two there's two details to how API server can communicate to connectivity server uh one choice is whether to use a Unix domain or a like a TLS connection local uh and we we used Unix sockets in instead of the other and then there's a protocol choice between whether to use grpc or HTTP connect and the advantage of HTTP connect is that it's an open standard and you could use that mode that egress selector configuration mode if you wanted to completely replace the reference implementation with your own implementation um using grpc mode sort of locks you into really implementing implementing the specific connectivity um Proto message Api right than you you're welcome I love questions more questions thanks for the talk uh I'm Tom with Sony so uh what's going to be the next step or what's missing for the connectivity because that that it does seem to be pretty much done for me I I maybe missed it interesting you know the what's next um if you go watch the 2019 talk uh there's there's further detail into sort of maybe what more advanced features and what could be next I sort of agree with you from my perspective we should just focus on really really um locking down what appears to me to be a stable basic dial flow um and and then call that GA but one in the community part of the path to GA that we're discussing we need to have a better regression test combination and Matrix that will test across version skew right now all of our testing is kind of unit testing within a repo and so we don't currently have regression tests against a cube API server with a compiled in connectivity client library at an older version with a newer connectivity server and there's also theoretically a connectivity agent so you've got three different um binaries that you could have version skew across uh and the test the test Matrix combination against all three um so there's that uh another Gap um I think I already mentioned there's a desire to simplify the protocol what a proxy connection identifier is uh right now there's kind of a twostep identifier pending dials have a different distinct identif and established connections have a diff have like a different one uh and there's a proposal to have it be the same identifier for the whole lifetime of the the the dial flow which I would personally like to see another Gap that we would like for the path to GA is that the grpc connection here on the diagram from the agent to the server today is truly multiplexed so one agent grpc connection is multiplexing um any arbitrary number of proxy TCP connections but the arrow the grpc connection from API server to connectivity server is actually single use which is very expensive my understanding is that during prototyping they had a version that was truly multiplexed but unfortunately when HT HTTP connect mode was added they kind of couldn't get both working and due to some changes related to that making it abstract they reduced the grpc code path to single use so it's it's um today's implementation um is greatly less efficient than it could be although I have to say in my experience in gke that's never really been a problem okay thank you all right if there are no more questions then thanks for your time and I hope this was **useful**